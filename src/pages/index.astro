---
import { companyConfig } from '../data/companyConfig';
---

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AirTUR - Strefa Komfortu | Strona w przygotowaniu</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
        @import "https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css";
        
        :root {
            /* TWOJE KOLORY FIRMOWE */
            --airtur-dark: #0f172a;
            --airtur-blue: #3b82f6; 
        }
        .bg-airtur-dark { background-color: var(--airtur-dark) !important; }
        .text-airtur-blue { color: var(--airtur-blue) !important; }
        .bg-airtur-blue { background-color: var(--airtur-blue) !important; }
        .border-airtur-blue { border-color: var(--airtur-blue) !important; }
        .bg-blue-glow { background-color: rgba(59, 130, 246, 0.1); }
    </style>
</head>
<body class="min-h-screen flex flex-col bg-airtur-dark text-white font-sans overflow-hidden">
    
    <header class="bg-white py-6 shadow-md relative z-20">
      <div class="max-w-7xl mx-auto px-4 text-center">
        <img src="/logo.svg" alt="AirTUR Logo" class="h-12 md:h-16 mx-auto" />
      </div>
    </header>

    <main class="flex-grow flex items-center justify-center relative bg-airtur-dark">
      
      <div class="absolute inset-0 opacity-20">
          <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-airtur-blue rounded-full blur-3xl filter blur-[100px]"></div>
          <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-airtur-blue rounded-full blur-3xl filter blur-[100px]"></div>
      </div>

      <div class="max-w-3xl mx-auto px-4 text-center relative z-10 py-12">
        <div class="mb-8">
          <div class="inline-block px-4 py-1 rounded-full border border-airtur-blue bg-blue-glow text-airtur-blue text-xs font-bold uppercase tracking-widest">
              Systemy klimatyzacji & pomp ciepła
          </div>
        </div>
        
        <h1 class="text-4xl md:text-6xl font-black mb-6 tracking-tight uppercase">
          Trwa <span class="text-airtur-blue">optymalizacja</span><br/>naszej witryny
        </h1>
        
        <p class="text-gray-400 text-lg md:text-xl mb-12 leading-relaxed max-w-2xl mx-auto">
          Pracujemy nad nowym standardem prezentacji naszych systemów. 
          Wkrótce udostępnimy pełną ofertę AirTUR.
        </p>

        <div class="bg-white/5 border border-white/10 rounded-3xl p-8 md:p-10 mb-8 backdrop-blur-sm">
          <p class="text-airtur-blue font-bold uppercase tracking-widest text-sm mb-4">Jesteśmy dostępni pod telefonem:</p>
          <a href={`tel:${companyConfig.phone}`} class="text-3xl md:text-5xl font-extrabold hover:text-airtur-blue transition-colors">
              {companyConfig.phone}
          </a>
          <div class="mt-6 pt-6 border-t border-white/10 flex flex-col md:flex-row justify-center gap-6 text-sm text-gray-400 font-bold uppercase tracking-widest">
              <div class="flex items-center justify-center gap-2">
                  <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                  Przyjmujemy zapytania ofertowe
              </div>
              <div>
                  E-mail: {companyConfig.email}
              </div>
          </div>
        </div>

        <p class="text-xs text-gray-500 uppercase tracking-widest">
          © {new Date().getFullYear()} AirTUR Inżynieria Komfortu
        </p>
      </div>
    </main>
</body>
</html>