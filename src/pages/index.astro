---
import { companyConfig } from '../data/companyConfig';
---

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AirTUR - Strefa Komfortu | Strona w przygotowaniu</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
        @import "https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css";
        /* Jeśli używasz specyficznych kolorów AirTUR, zdefiniujemy je tutaj tymczasowo */
        :root {
            --airtur-dark: #0f172a;
            --airtur-blue: #3b82f6; 
        }
        .bg-airtur-dark { background-color: var(--airtur-dark); }
        .text-airtur-blue { color: var(--airtur-blue); }
        .border-airtur-blue { border-color: var(--airtur-blue); }
    </style>
</head>
<body class="min-h-screen flex flex-col bg-gray-900 text-white font-sans overflow-hidden">
    
    <header class="bg-white py-6 shadow-md relative z-20">
      <div class="max-w-7xl mx-auto px-4 text-center">
        <img src="/logo.svg" alt="AirTUR Logo" class="h-12 md:h-16 mx-auto" />
      </div>
    </header>

    <main class="flex-grow flex items-center justify-center relative bg-gray-900">
      
      <div class="absolute inset-0 opacity-20">
          <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-blue-600 rounded-full blur-3xl"></div>
          <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-blue-600 rounded-full blur-3xl"></div>
      </div>

      <div class="max-w-3xl mx-auto px-4 text-center relative z-10 py-12">
        <div class="mb-8">
          <div class="inline-block px-4 py-1 rounded-full border border-blue-500/30 bg-blue-500/10 text-blue-400 text-xs font-bold uppercase tracking-widest">
              Systemy klimatyzacji & pomp ciepła
          </div>
        </div>
        
        <h1 class="text-4xl md:text-6xl font-black mb-6 tracking-tight uppercase">
          Trwa <span class="text-blue-500">optymalizacja</span><br/>naszej witryny
        </h1>
        
        <p class="text-gray-400 text-lg md:text-xl mb-12 leading-relaxed max-w-2xl mx-auto">
          Pracujemy nad nowym standardem prezentacji naszych systemów. 
          Wkrótce udostępnimy pełną ofertę AirTUR.
        </p>

        <div class="bg-white/5 border border-white/10 rounded-3xl p-8 md:p-10 mb-8">
          <p class="text-blue-500 font-bold uppercase tracking-widest text-sm mb-4">Jesteśmy dostępni pod telefonem:</p>
          <a href={`tel:${companyConfig.phone}`} class="text-3xl md:text-5xl font-extrabold hover:text-blue-400 transition-colors">
              {companyConfig.phone}
          </a>
          <div class="mt-6 pt-6 border-t border-white/10 flex flex-col md:flex-row justify-center gap-6 text-sm text-gray-400 font-bold uppercase tracking-widest">
              <div class="flex items-center justify-center gap-2">
                  <span class="w-2 h-2 bg-green-500 rounded-full"></span>
                  Przyjmujemy zapytania ofertowe
              </div>
              <div>
                  E-mail: {companyConfig.email}
              </div>
          </div>
        </div>

        <p class="text-xs text-gray-500 uppercase tracking-widest">
          © {new Date().getFullYear()} AirTUR Inżynieria Komfortu
        </p>
      </div>
    </main>
</body>
</html>